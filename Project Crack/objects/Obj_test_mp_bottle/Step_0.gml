/// @description 在此处插入描述 
// 你可以在此编辑器中写入代码
if place_meeting(x,y,Obj_SafeObstacle) == false{
	x += x_var*inverse
	y_var -= 0.5
	y -= y_var

}
if place_meeting(x,y,Obj_SafeObstacle){
	if y < Obj_SafeObstacle.y{
		y